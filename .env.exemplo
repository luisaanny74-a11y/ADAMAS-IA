# ══════════════════════════════════════════════════════════
# ADAMAS — Modelo de Variáveis de Ambiente
# Copie este arquivo para .env e preencha com seus valores reais
# NUNCA commite o arquivo .env — apenas este exemplo
# ══════════════════════════════════════════════════════════

# Chave secreta do Flask — protege as sessões de login
# Gere uma chave segura com:
# python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=cole_sua_chave_secreta_aqui
